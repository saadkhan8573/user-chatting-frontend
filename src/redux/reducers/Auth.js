import { userSlice, LoginSlice, UsersGet } from '../slice';

export const userReducer = userSlice.reducer;
export const loginReducer = LoginSlice.reducer;
export const UsersGetReducer = UsersGet.reducer;